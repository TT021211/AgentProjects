# 项目维护指南

> **最后更新**：2026-06-10  
> **定位**：AI/智能体的行为引导 + 项目文档维护流程。可跨项目复用。

本文档定义 AI/智能体在项目中的全部行为准则：如何进入项目、如何理解代码、如何生成文档、如何维护文档。遵循本指南可确保文档与代码始终同步，任何接手者都能快速上手。

---

## 〇、进入项目时的初始行为

AI/智能体首次进入项目时，必须按以下顺序执行：

### 步骤 1：理解项目

```
1. 读 项目维护.md（本文档）→ 理解行为准则
2. 读 package.json / pom.xml → 了解技术栈和构建方式
3. 读构建配置文件 → 了解框架和运行配置
4. 遍历目录结构 → 识别模块划分
5. 列出 doc/md/ 目录下所有 .md 文件 → 了解项目文档全貌。所有 md 文件与 项目维护.md 在同一目录，列出后可快速了解有哪些模块文档、专题文档、接口文档等。如有必要（如首次进入、项目交接、大规模重构前），通读所有 md 文件以全面理解项目
6. 读 `{前缀}-api.md` → 了解所有接口定义（入参/出参/路径/格式），**编码前必须读**。若该文件不存在，告知用户并请求提供；用户仍未提供则跳过
7. 读 知识库_Dep.md → 了解历史开发中犯过的错误和积累的经验教训。该文件是项目级别的"踩坑记录"，涵盖 MongoDB 冗余字段、MyBatis-Plus null 更新、cron 表达式格式、日期类型处理等实际问题，编码前通读可避免重蹈覆辙
8. 读已有的模块 md 文件 → 了解业务逻辑
```

### 步骤 2：检查并补齐文档

```
1. 检查 {前缀}-pro.md 是否存在
   → 不存在：根据「第二章」自动生成
   → 存在但不完整：补充缺失章节

2. 检查模块文档是否存在（命名规则见「第五章」）
   → 不存在：按「第三章」逐个模块分析并生成
   → 内容过时：根据最新代码更新

3. 检查文档完整性
   → 每个模块的变更记录是否最新
   → 接口清单是否与实际代码一致
```

---

## 一、AI/智能体行为准则

**沟通原则**：所有回复尽量简洁，节省 token。AI/Agent 不得展示思考过程（`<thinking>` 块），直接输出结果。

### 1.1 编码前（铁律）

| 检查项 | 动作 |
|--------|------|
| 存在 `{前缀}-pro.md` | 通读，了解项目技术规范和编码风格 |
| 存在 知识库_Dep.md | 通读，了解历史踩坑记录，避免重蹈覆辙 |
| 存在模块文档 | 读相关模块文档，理解已有业务逻辑和数据流 |
| 不确定改动范围 | 先 grep 全量搜索，确认所有受影响的文件 |
| 多套实现的页面 | 确认是否需同步修改（如 Tab 合并页 + 独立页） |

**未读相关文档前，不得开始编码。**

### 1.2 编码后（必须）

| 动作 | 时机 |
|------|------|
| 更新模块 `{前缀}-*.md` | 新增页面、修改接口、调整核心流程时 |
| 更新 `{前缀}-pro.md` | 新增公共组件、修改编码规范、调整项目结构时 |
| 追加变更记录 | 每次文档修改后，在对应文档底部追加一行 |
| 运行质量自检 | 对照 `{前缀}-pro.md` 中的自检清单逐项确认 |
| grep 确认旧值已全部替换 | 字段/路径/变量重命名时 |
| 检查同功能多套实现 | 确认 Tab 合并页 + 独立页已同步修改 |

### 1.3 编码中（必须遵守的开发规范）

以下规范是底线要求，`{前缀}-pro.md` 中可在此基础上进一步细化。

#### 前端

| 规范 | 要求 |
|------|------|
| 页面组织 | 必须按模块分文件夹，同一业务域页面放同一目录 |
| 公共组件 | 提取到 `components/`，每个组件顶部必须有注释（用途、Props、Events、使用示例） |
| 公共方法 | 提取到 `common/` 或 `utils/`，每个方法必须有注释 |
| API 调用 | 使用项目统一封装（非原始请求），接口路径在配置文件中定义 |
| 样式 | 使用 scoped 样式，禁止行内样式 |
| 弹框 | 必须使用项目自定义弹框组件（多场景适配）。禁止使用：系统原生 alert/confirm/prompt；UniApp 原生 `<picker>` / `uni.showActionSheet` / `uni.showToast`(超 1s)；UI 框架默认弹框组件。选择器、确认框、输入框等**所有浮层类交互**统一走 bottom-sheet 或 x-popup |
| 注释密度 | 多步业务流程（5+ 行）每步注释；复杂算法密集注释；跨页面通信必加注释 |

> **弹框规范**：前端项目启动时需先设计和实现自定义弹框组件，覆盖确认、提示、输入、表单提交、列表选择等场景。判断标准：只要会"弹出浮层覆盖在当前页面之上"的交互，不论叫弹框、选择器、抽屉还是动作面板，一律走自定义组件。后续所有需要弹框的地方，统一调用此组件，统一交互体验和样式。

#### 后端

| 规范 | 要求 |
|------|------|
| 分层架构 | Controller 薄层（参数接收 + 响应封装）→ Service 承载业务 → Mapper/Repository 持久化 |
| 实体类 | 必须标注存储介质（`@TableName` MySQL / `@Document` MongoDB），每个字段必须有注释 |
| 关联字段 | 注释中标注关联的目标表/集合 |
| 状态字段 | 注释中列出所有枚举值及含义 |
| Controller | 每个方法必须有注释（用途 + 入参 + 出参），返回值统一封装 |
| 注释密度 | 常规业务 20 行 : 1 行注释；复杂算法 10 行 : 1 行注释；边界条件每条必注释（说明 why） |
| DTO/枚举 | 每个字段必注释 |
| 数据库 | 表有 COMMENT，字段有 COMMENT，DDL 纳入版本管理 |
| UML 类图 | 每个模块生成 PlantUML 类图，放在 `uml/` 目录 |

#### 通用

| 规范 | 要求 |
|------|------|
| API 路径 | 禁止硬编码，统一在配置文件中定义 |
| 错误码 | 使用枚举，禁止硬编码数字 |
| 提交信息 | `[模块] 简短描述` 格式 |
| 接口文档 | 前后端开发必须严格按 `{前缀}-api.md` 中定义的入参/出参/路径/格式执行，不允许自行猜测或偏离 |
| 质量自检 | 编码完成后对照 pro.md 中的自检清单逐项确认 |

---

## 二、{前缀}-pro.md 生成指南

AI/智能体在项目无 `{前缀}-pro.md` 时，必须根据本章自动生成。

### 2.1 必须包含的章节

| 章节 | 内容 | 详细程度 |
|------|------|---------|
| **项目概述** | 项目定位、技术栈、支持的平台、依赖清单 | 一页以内 |
| **项目结构** | 完整目录树、每个目录的用途说明 | 覆盖所有非 node_modules 的一级目录 |
| **启动与生命周期** | 入口文件启动流程、根组件结构、生命周期钩子逻辑 | 关键代码片段 + 流程图 |
| **页面路由** | 完整路由表（路径、页面、标题） | 所有页面逐一列出 |
| **公共组件** | 每个组件的 Props/Events/Slots/使用示例 | 至少含名称、参数表格、一个示例 |
| **公共工具** | 每个 .js 模块的用途、导出方法、使用示例 | 方法签名 + 一句话说明 |
| **状态管理** | 数据存储方案、流转模式、持久化方式 | 含所有存储 key |
| **接口调用规范** | HTTP 封装方式、Token 注入、错误处理、响应格式 | 代码示例 |
| **编码规范** | 命名规范（文件/变量/函数/常量）、文件组织、代码风格 | 表格化 |
| **构建与部署** | 打包命令、环境配置、发布流程 | 命令行示例 |
| **接口文档** | **后端项目必填**：所有 REST 接口的路径、方法、入参、出参、错误码 | 按 Controller 分组，每个接口一行 |

### 2.2 生成方式

AI 必须通过**阅读项目实际代码**来生成，不允许编造不存在的内容。

| 项目类型 | 必须读的文件 |
|---------|-------------|
| uni-app 前端 | `main.js`、`App.vue`、`pages.json`、`manifest.json`、`common/config.js`、`common/store.js`、`common/request.js` |
| Vue/React 管理后台 | `main.js`、`App.vue`、`router/index.js`、`vite.config.js`、`api/`、`stores/` |
| Spring Boot 后端 | `pom.xml`、`Application.java`、所有 `controller/` 文件（生成接口文档必须通读每个 Controller）、核心 `service/impl/` 文件 |

---

## 三、模块文档生成指南

### 3.1 何时生成

每个功能模块（页面数 ≥ 2）生成独立的模块文档，命名规则见「第五章」。

### 3.2 识别模块边界

```
1. 从目录结构识别模块分组（如 pages/ 下的子目录或 src/ 下的 package）
2. 从路由配置识别页面清单
3. 从 API 配置文件识别接口分组
4. 每个功能领域划分为一个模块
```

### 3.3 深入分析维度

对每个模块，必须从以下维度逐一分析代码：

| 分析维度 | 具体方法 |
|---------|---------|
| **页面清单** | 读路由配置 + 目录结构，列出该模块所有页面 |
| **数据流** | 读每个页面的 data/form/state 定义，理解数据结构 |
| **业务逻辑** | 读 methods/computed/watch，理解核心流程和状态流转 |
| **API 调用** | 从代码中找到所有接口调用，整理为清单 |
| **跨模块通信** | 识别 URL 参数传递、事件总线、全局状态等模式 |
| **特殊设计** | 标注与众不同的设计决策、复杂算法逻辑 |
| **已知问题** | 标注代码中的 TODO、不一致、潜在 bug |

### 3.4 必须包含的章节

| 章节 | 内容 |
|------|------|
| **文档维护指南** | 什么属于该模块、怎么登记变更 |
| **模块概述** | 功能定位 + 页面清单（文件/标题/路由/职责） |
| **核心业务概念** | 状态定义、数据结构、关键流程（ASCII 流程图） |
| **页面详情** | 每个页面的 UI 结构、表单结构、核心逻辑、API 调用 |
| **接口清单** | 该模块涉及的所有 API，按功能分组 |
| **跨页面通信** | URL 参数、事件总线、全局状态等方式 |
| **架构问题与建议** | 不一致、缺失、TODO、设计缺陷 |
| **变更记录** | 日期 + 变更内容 |

### 3.5 生成方式

AI 必须**逐个页面通读代码**，提取实际逻辑，不允许推测或编造。

---

## 四、文档维护规范

### 4.1 什么时候更新文档

| 触发条件 | 更新哪个文件 |
|---------|------------|
| 新增页面 | 对应模块的 `{前缀}-*.md`（页面清单 + 页面详情） |
| 修改接口参数/响应 | 对应模块的 `{前缀}-*.md`（接口清单 + 相关页面章节） |
| 新增公共组件 | `{前缀}-pro.md`（公共组件章节） |
| 修改数据结构 | 对应模块的 `{前缀}-*.md`（数据结构章节） |
| 修改核心流程 | 对应模块的 `{前缀}-*.md`（流程图 + 逻辑描述） |
| 修改构建方式 | `{前缀}-pro.md`（构建与部署章节） |
| 修改编码规范 | `{前缀}-pro.md`（编码规范章节） |

### 4.1.1 后端项目接口文档（强制）

后端项目必须将所有接口输出至 `{前缀}-api.md`，要求：

1. **完整性**：每个 Controller 的每个 `@PostMapping` 逐一登记，不遗漏任何接口
2. **入参**：每个字段标注类型、必填/非必填、说明；嵌套字段展开子表格
3. **出参**：每个字段标注类型、说明；嵌套结构逐层展开，不写"同 xx 接口"
4. **复杂接口重点描述**：逻辑度高（多表操作、状态流转、异步任务）的接口，必须在末尾用 `>` 引用块补充业务规则、校验逻辑、副作用
5. **枚举值**：状态、类型等字段必须列出所有可能值及含义，如 `status: 0-待采购 1-采购中 2-已完成`，禁止只写"状态"二字
6. **禁止简化描述**：禁止使用"返回完整信息"、"同 xx 接口"、"按分页返回"等无具体内容的描述，必须逐字段列出。列表接口必须展开 `records[]` 子表格
7. **对照检查**：每次修改接口后，必须检查 api.md 中对应接口的入参/出参是否与代码实际返回一致，不一致则立即更正
8. **接口日志中文描述**：新增接口时，必须在 `ControllerLogAspect.java` 的 `URI_DESC` 静态映射中追加该接口的中文描述，确保日志中 `REQ >> /path [中文说明]` 可读

### 4.2 变更记录格式

每次修改文档后，在文档底部的变更记录表中追加：

```markdown
| YYYY-MM-DD | 变更内容 |
```

### 4.3 编写原则

1. **准确性**：文档反映代码的实际状态，不得描述未实现的功能
2. **完整性**：新页面/接口上线后补齐文档，不遗漏
3. **可追溯**：重大逻辑变更在底部变更记录追加条目
4. **结构化**：使用表格、列表、代码块，避免大段散文
5. **中文为主**：技术术语使用英文原词，其余中文描述

---

## 五、文档命名规范

### 5.1 前缀确定规则

模块文档的前缀来源于项目名称，由 AI/智能体在首次进入项目时自动确定：

| 步骤 | 说明 |
|------|------|
| 1. 读取 `package.json` 的 `name` 字段（前端）或 `pom.xml` 的 `artifactId`（后端） |
| 2. 提取项目简称（如 `FM-Ass` → `ass`，`FM-Manage` → `manage`，`FastMes` → `fastmes`） |
| 3. 转为全小写、无特殊字符的短标识符 |
| 4. 将此标识符作为该项目的模块文档前缀 |

**示例**：

| 项目名 | 前缀 | 模块文档命名 |
|--------|------|-------------|
| FM-Ass | `ass` | `ass-product.md`、`ass-order.md` |
| FM-Manage | `manage` | `manage-dashboard.md`、`manage-version.md` |
| MyShop | `myshop` | `myshop-product.md`、`myshop-order.md` |

> **规则**：前缀在项目初始化时确定一次，之后不再更改。所有模块文档使用统一前缀。

### 5.2 文档类型与命名

| 类型 | 命名 | 示例 |
|------|------|------|
| 项目维护指南 | `项目维护.md` | — |
| 项目技术文档 | `{前缀}-pro.md` | — |
| 模块业务文档 | `{前缀}-[module].md` | `ass-product.md` |
| 专题设计文档 | `[topic].md` | `login.md` |

---

## 六、项目文档标准集合

以 FastMes 项目为例（前缀 = `fastmes`），完整维护的项目应包含：

```
FastMes/
├── doc/md/
│   ├── 项目维护.md               # AI/智能体行为引导 + 文档维护流程（必备）
│   ├── fastmes-pro.md            # 项目技术规范 ✅
│   ├── fastmes-plan.md           # 排产模块 ✅
│   ├── fastmes-api.md            # API 接口说明书 ✅
│   ├── fastmes-order.md          # 订单管理模块 ✅
│   ├── fastmes-customer.md       # 客户管理模块 ✅
│   ├── fastmes-product.md        # 产品管理模块 ✅
│   ├── fastmes-supplier.md       # 采购管理模块 ✅
│   ├── fastmes-enterprise.md     # 企业管理模块 ✅
│   ├── fastmes-production.md     # 生产管理模块 ✅
│   ├── fastmes-overview.md       # 项目业务概述
│   ├── login.md                  # 扫码登录设计
│   ├── fileUpload.md             # 文件上传设计
│   └── 知识库_Dep.md   # 开发经验知识库 ✅
├── doc/sql/                      # SQL DDL
├── uml/                          # PlantUML 类图
└── src/
```

---

## 七、收尾规则

当用户说"收尾"或"今日工作收尾"时，AI 必须执行以下操作：

1. 回顾本次对话中所有**新增/修改的代码**，逐项对照本文档第四章「文档维护规范」，将变更写入对应文档
2. **接口变更** → 更新 `{前缀}-api.md`（入参/出参/路径/错误码）
3. **数据表/字段/索引变更** → 更新对应 `doc/sql/init_*.sql` 文件
4. **新增实体字段** → 更新实体类 Javadoc + 对应 UML 类图
5. **修改业务流程** → 更新对应模块的 `{前缀}-[module].md`
6. **新增公共组件/工具** → 更新 `{前缀}-pro.md`
7. 应在文档底部**追加变更记录**行
8. **复盘本次错误** → 回顾开发中犯过的错，提炼为经验教训，写入 `知识库_Dep.md`。若本次无犯错则跳过此步
9. 最终编译确认无报错

> 收尾的核心原则：**本次对话的改动，不得留到下次对话再整理文档**。
