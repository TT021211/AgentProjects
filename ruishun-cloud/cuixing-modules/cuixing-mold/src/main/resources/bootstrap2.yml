# Tomcat
server:
  port: 9093

# Spring
spring:
  application:
    # 应用名称
    name: huaerda-mold
  profiles:
    # 环境配置
    active: ${SPRING_PROFILES_ACTIVE}
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: ${NACOS_HOST:127.0.0.1}:${NACOS_PORT:18848}
        namespace: ${NACOS_NAMESPACE:ea10f8a7-4839-4beb-86ce-b5ddf9c4add3}
#        ip: ${LOCAL_IP_ADDRESS:192.168.9.237}
      username: ${NACOS_USERNAME:nacos}
      password: ${NACOS_PASSWORD:nacos}
      config:
        # 配置中心地址
        server-addr: ${NACOS_HOST:127.0.0.1}:${NACOS_PORT:8848}
        namespace: ${NACOS_NAMESPACE:ea10f8a7-4839-4beb-86ce-b5ddf9c4add3}
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-${SPRING_PROFILES_ACTIVE}.${SPRING_CLOUD_NACOS_CONFIG_FILE_EXTENSION}
