declare class WwLogin {
  constructor(options: {
    id: string;
    appid: string;
    agentid: string;
    redirect_uri: string;
    state: string;
    href: string;
    lang: string;
  });

  // 如果需要，可以添加其他方法和属性的声明
}
